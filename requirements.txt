fastapi>=0.110
uvicorn[standard]>=0.29
fonttools>=4.50
pydantic>=2.6
resvg-py>=0.5

# Binario portatil de ffmpeg, usado solo si no hay ffmpeg en el PATH del
# sistema (por ejemplo en Vercel, que no lo trae instalado). Ver CLAUDE.md #7.
imageio-ffmpeg>=0.5

# Alternativa al rasterizador, por si resvg no tiene rueda para tu sistema.
# En Windows necesita ademas las DLL de Cairo (GTK3 Runtime).
# cairosvg>=2.7
